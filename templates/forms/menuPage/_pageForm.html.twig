{{ form_start(form, {'attr': {'class': 'admin-form__container'}}) }}

<div class="admin-form__container__edit">

	{{ form_row(form.title) }}
	{{ form_row(form.titleIsDisplayed) }}


	{{ form_row(form.description) }}
	{{ form_row(form.pageIsDisplayed) }}

	{{ form_row(form.pageOrder) }}

</div>

<button class="admin-form__container__confirm-edit-bttn page-theme">
	{{ button_label|default('Save') }}
</button>

{{ form_end(form) }}
